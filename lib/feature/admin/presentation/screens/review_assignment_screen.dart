import 'package:flutter/material.dart';

class ReviewAssignmentScreen extends StatelessWidget {
  const ReviewAssignmentScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      body: Center(child: Text('Review Assignment ')),
    );
  }
}