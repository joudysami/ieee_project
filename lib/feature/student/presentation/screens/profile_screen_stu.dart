import 'package:flutter/material.dart';

class ProfileScreenStu extends StatelessWidget {
  const ProfileScreenStu({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
